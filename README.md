# RestFulAPIsWithPython

===
#  Prepare environment
python3 -m venv venv
source venv/bin/activate
#deactivate
pip install flask
#a directory with name venv will be created inside your repo directory

#to run flask app, this will run code inside app.py
sudo FLASK_APP=app.py Flask run
